"use client";

import type { VehicleDetailProps } from "@/components/themes/types";

// Stub — replaced in "carmax theme: DetailView surface".
export default function DetailView(_props: VehicleDetailProps) {
  return <div />;
}
